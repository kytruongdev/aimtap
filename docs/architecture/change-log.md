# Change Log — tài liệu kiến trúc

Ghi lại thay đổi đối với tài liệu đã được phê duyệt. Thay đổi trước thời điểm phê duyệt được sửa trực tiếp vào tài liệu và không ghi ở đây.

| Ngày | Nội dung | Tài liệu ảnh hưởng | Team Lead cần đánh giá lại? |
|---|---|---|---|
| 2026-07-22 | Phê duyệt bộ tài liệu Giai đoạn 0: kiến trúc tổng thể, quy ước viết mã, và tám quyết định kiến trúc nền tảng. Mốc bắt đầu áp dụng quản lý thay đổi. | `north-star.md`, `coding-convention.md`, `adr/adr-001.md` → `adr/adr-008.md` | — |
| 2026-07-22 | Đồng bộ với định nghĩa "bằng chứng thực thi" ba phần ở `brd.md` §1: trách nhiệm Evidence Collector và nguyên tắc thu thập bằng chứng phát biểu theo cùng ba phần đó. Gắn AS-05 làm căn cứ cho NFR-10 và cho ngưỡng chuyển sang chụp ảnh ở mọi bước. | `north-star.md` §2, §2.2, §5, §6 | Không. Trách nhiệm module và hợp đồng dữ liệu không đổi; chỉ thống nhất cách phát biểu và bổ sung truy vết. |
| 2026-07-23 | Chốt ADR-009 (Accepted): lưu dữ liệu kiểm thử và bí mật ngoài kho mã bằng tệp có cấu trúc theo từng ứng dụng, một tệp cho mỗi ứng dụng (`apps/<app-id>/test-data.{example,local}.json`). | `adr/adr-009.md` | Là đầu vào khi chẻ ticket khai báo ứng dụng và Config & Secrets. |
| 2026-07-23 | Chốt ADR-010 (Accepted): kiểm tra thiết bị giữa các test case bằng probe nhẹ trên phiên Appium đang mở, không kiểm tra lại ở tầng hệ điều hành mỗi test case. Phép kiểm tra đầy đủ FR-DEV-02 giữ nguyên ở bước mở lượt chạy. | `adr/adr-010.md` | Là đầu vào khi chẻ ticket Test Runner và Device & Build Manager. |
| 2026-07-23 | Đồng bộ từ vựng trung tâm mới ("kịch bản" → test case; "bộ kịch bản" → test suite; bổ sung tầng test feature; "Reviewer kịch bản" → Reviewer) và hợp đồng dữ liệu (thêm tên test feature và thông báo lỗi gốc; loại lỗi hai giá trị theo BR-014; test case không chạy không sinh bản ghi) vào toàn bộ tài liệu Giai đoạn 0. Hấp thụ cấu trúc ADR-009 vào `north-star.md` §2.1 và `coding-convention.md`; chỉ mục ADR-009/ADR-010 vào `north-star.md` §3; thêm NFR-12 vào §5. | `north-star.md`, `coding-convention.md`, `adr/adr-001.md` → `adr/adr-008.md` | Có. Hợp đồng dữ liệu kết quả và quy ước viết mã đổi cách gọi tên và bổ sung trường; dùng bản mới khi chẻ ticket Phase 1. |
| 2026-07-23 | Hoàn tất thiết kế chi tiết Phase 1: `component-design.md`, `erd.md`, `interface-spec.md`, `sequence-diagrams.md`. Chốt ADR-011 (tên màn hình lấy từ Page Object) và ADR-012 (Puppeteer kèm Chromium đóng gói) là quyết định kỹ thuật của SA. Product owner xác nhận phạm vi Phase 1 ở mức nghiệp vụ; ADR-012 giữ phương án tải Chromium đóng gói. | `phase-1/*.md`, `adr/adr-011.md`, `adr/adr-012.md` | Có. Đây là bộ thiết kế Team Lead tiếp nhận để chẻ ticket Phase 1. |
| 2026-07-23 | Chốt ADR-013 (Accepted): mô hình thực thi Test Runner — WebdriverIO/Cucumber điều khiển việc lặp qua test case, nền tảng phản ứng qua hook vòng đời; `saveRunStart`/`finalizeRun` ở hook `before`/`after` cấp phiên worker; dừng giữa chừng = cờ dừng + bỏ qua scenario còn lại (không sinh bản ghi); hủy qua SIGINT. Làm rõ `component-design.md` §Test Runner và `sequence-diagrams.md` §1, §2, §3; ADR-010 nêu probe ở `beforeScenario`. | `adr/adr-013.md`, `phase-1/component-design.md`, `phase-1/sequence-diagrams.md`, `adr/adr-010.md` | Có. TICKET-018/019 điều chỉnh diễn đạt theo ADR-013. |
| 2026-07-24 | Chốt ma trận phụ thuộc module Phase 1 (chu trình lộ ra khi dựng ranh giới lint ở US-1.1). Gỡ chu trình Test Runner ↔ Locator Resolver bằng ADR-014: Locator không import Test Runner, dùng phiên WebdriverIO toàn cục và nhận tên màn hình qua sink Test Runner tiêm lúc mở phiên; một chiều Test Runner → Locator. Bổ sung phụ thuộc còn thiếu ở §2: CLI → App Registry, Reporter; Test Runner → Device & Build Manager, Locator Resolver; Device → App Registry (kiểu AppConfig); Shared là kernel mọi module được import. Chỉ mục ADR-011→014 vào §3. | `north-star.md` §2/§2.1/§3, `phase-1/component-design.md`, `phase-1/interface-spec.md`, `adr/adr-014.md` | Có. Ma trận là đầu vào để chốt `eslint.config.ts`; diễn đạt US-2.1 (Locator) và US-3.4 (Runner) theo ADR-014. |
| 2026-07-24 | Đồng bộ thiết kế theo phát hiện lúc review code US-1.4/US-3.1: (1) `interface-spec` §Result Store phát biểu rõ append-only — bản ghi kết quả (TestCaseResult, StepLog) insert-only; hàng tổng hợp của lượt chạy hoàn tất một lần bằng `finalizeRun`. (2) Composition tiền điều kiện Device & Build Manager: `checkEnvironment` không kèm target kiểm host tools (Node/Xcode/Appium), `ensureReadyBeforeRun` sở hữu thiết bị/OS/bản build → `DeviceContext`, không kiểm trùng; `sequence-diagrams` §1 thêm bước `checkEnvironment`. (3) Thêm `Result Store` vào phụ thuộc Test Runner ở §2 và `component-design` (khớp `eslint.config.ts`: `runner → store`). | `phase-1/interface-spec.md`, `phase-1/sequence-diagrams.md`, `phase-1/component-design.md`, `north-star.md` §2 | Có. Đầu vào wiring chuỗi tiền điều kiện ở TICKET-021 (US-4.3); AC "không cập nhật/xóa" của TICKET-007 chỉnh theo cách phát biểu append-only mới. |
| 2026-07-24 | Chốt ADR-015 (Accepted, Product Owner duyệt): `wait-policy` là hạ tầng Shared, không thuộc Locator Resolver. Phát hiện khi hiện thực TICKET-010 (probe bọc trong `wait-policy`, ADR-010): Device import `wait-policy` từ `locator/` tạo cạnh `device → locator`, vi phạm ma trận ADR-014. Chuyển `wait-policy.ts` (`WaitPolicy`, `DEFAULT_WAIT_POLICY`, get/configure/reset, `withRetries`) sang `src/shared/`; Locator và Device đều import từ Shared. Ma trận ADR-014 và `eslint.config.ts` không đổi (không cạnh mới). `component-design` §Shared/§Locator và `north-star` §2/§2.1/§3 cập nhật. | `adr/adr-015.md`, `north-star.md` §2/§2.1/§3, `phase-1/component-design.md` | Có. Di chuyển tệp + sửa import thuộc TICKET-010/US-2.1: `src/locator/wait-policy.ts` → `src/shared/` (kèm test), cập nhật re-export ở `locator/index.ts` và `shared/index.ts`, sửa import ở `locator-resolver.ts`. |
| 2026-07-30 | Chốt ADR-016 (Accepted, Product Owner duyệt): thêm discriminant `kind` trên `AppFailure` (`step_execution` mặc định | `assertion`) để `failure-classifier` phân `failure_type` (BR-014) không cần match chuỗi lỗi — `assertion` → `wrong_conclusion`; `step_execution` và lỗi lạ không thuộc `AppFailure`/`PlatformFailure` → `step_not_executed`; luôn giữ `error_message` gốc; `PlatformFailure` không ghi thành test case hỏng. `find` (US-2.1) đã phù hợp nhờ mặc định — không sửa code đã merge. Cập nhật `component-design` §Shared/§Evidence Collector/§Locator. | `adr/adr-016.md`, `phase-1/component-design.md` | Có. Áp vào TICKET-015 (US-2.2): thêm trường `kind` ở `shared/errors.ts`, classifier ánh xạ, cơ chế khẳng định đặt `kind='assertion'`. |
| 2026-08-11 | Chốt ADR-019 (Accepted, Product Owner duyệt) thay ADR-012: báo cáo là một tệp HTML tự chứa (ảnh nhúng data URI), gỡ Puppeteer/Chromium; `render` đồng bộ, không tham số `format`. Nội dung báo cáo (ADR-006) không đổi. | `adr/adr-019.md`, `adr/adr-012.md` (Superseded), `phase-1/component-design.md` §Reporter, `phase-1/interface-spec.md` §Reporter/§Jira, `north-star.md` §2/§2.1/§3/§4 | Có. Định dạng đính Jira (FR-REP-03) đổi PDF/PNG→HTML thuộc cập nhật tài liệu nghiệp vụ (BA). |
| 2026-08-11 | Chốt ADR-020 (Accepted, Product Owner duyệt) thay ADR-003: một cơ sở dữ liệu kết quả dùng chung `data/database.db` cho mọi ứng dụng (mỗi hàng mang `app_id`), thay per-app `output/<app-id>/results.db`; `data/` bền tách `output/` bỏ được; `openDatabase(dataDir)` bỏ tham số `app-id`; `findAppForRun` gỡ. Các lựa chọn kỹ thuật khác của ADR-003 giữ nguyên. | `adr/adr-020.md`, `adr/adr-003.md` (Superseded), `phase-1/component-design.md` §Result Store, `phase-1/interface-spec.md` §Result Store/§Reporter, `phase-1/erd.md`, `north-star.md` §2/§2.1/§3/§4 | Có. `.gitignore` thêm `data/` thuộc cập nhật cấu hình kho mã (PO). |
| 2026-08-20 | Chốt thiết kế Phase 2 **hướng B** (Product Owner duyệt): nền tảng gọi AI bằng subprocess một AI CLI ngoài (Claude Code), không nhúng AI/khóa API. ADR-025 (Accepted, thay ADR-005) — cổng `CodeAgent` + adapter subprocess `claude -p` + heal read-only trả chuỗi locator + generate; ADR-026 (Accepted) — token thuê bao `CLAUDE_CODE_OAUTH_TOKEN` trong env git-ignored + `make setup`/`doctor`; ADR-024 (Accepted) — thực thể `heal_event`, `test_case_result.status` còn passed/failed, "đạt kèm tự phục hồi" là nhãn dẫn xuất. Nền tảng KHÔNG tạo pull request — con người áp locator + mở PR sau review. | `adr/adr-024.md`, `adr/adr-025.md`, `adr/adr-026.md`, `adr/adr-005.md` (Superseded), `adr/adr-021.md`/`adr-022.md`/`adr-023.md` (Superseded, thăm dò hướng A), `phase-2/component-design.md`/`erd.md`/`interface-spec.md`/`sequence-diagrams.md`, `north-star.md` §1/§2/§2.1/§3/§4/§5/§6 | Có. Bộ thiết kế Phase 2 để Team Lead chẻ ticket. Phụ thuộc: BA đồng bộ chữ spec (bỏ auto-PR FR-HEAL-07/BR-210/US-203 + cập nhật tài liệu nền hướng B). |
| 2026-08-20 | Chốt ADR-027 (Accepted, Product Owner duyệt): kiểu `Locator` + enum `LocatorStrategy` là kiểu kernel ở `shared` (dùng chung `ai`/`locator`) — giải điểm ranh giới `ai → locator` Team Lead escalate khi chẻ US-7.2; bộ dựng iOS/`toSelector`/`describeLocator`/logic find giữ ở `locator` (re-export kiểu). Ma trận `{ from: ['ai'], allow: ['shared','config'] }` giữ nguyên (không cạnh `ai → locator`). Kèm đồng bộ chữ "Claude Client" (hướng A) → AI Gateway/`CodeAgent` gọi AI CLI ngoài ở `coding-convention.md` §Gọi-AI và `CLAUDE.md` §5. | `adr/adr-027.md`, `north-star.md` §2.1/§3, `coding-convention.md`, `CLAUDE.md` §5 | Có. Bước dời kiểu `Locator` lên `shared` (kiểu + enum, không phải bộ dựng) là bước hiện thực trong US-7.2 (Team Lead). |
| 2026-08-22 | Chốt ADR-028 (Accepted, Product Owner duyệt): bước generate chuyển sang **AI khám phá app agentic qua Appium MCP** — QC chỉ đưa DESC, AI viết `.feature` + tự lái phiên thiết bị sống inspect từng màn lấy locator + viết code; heal giữ ADR-024 (`invoke` một-lần, không agentic). Tinh chỉnh phần generate-mode của ADR-025 (không Superseded — heal + seam còn đúng). Hệ quả chấp nhận: lộ dữ liệu khi khám phá (page source mọi màn + dữ liệu test kể cả bí mật; PO chấp nhận cho Phase 2) và điểm thất bại E3 (khám phá kẹt màn). Khớp use-case BA re-settle (UC-203/US-206/FR-GEN-01/BR-211). | `adr/adr-028.md`, `adr/adr-025.md` §Hệ quả (con trỏ), `app-onboarding-workflow.md`, `onboarding-a-new-app.md` | Có. SA đã reconcile (2026-08-22) các artifact còn tả generate static PAGE: `phase-2/component-design.md` (§AI Gateway/§CLI Entry), `phase-2/interface-spec.md` (§CodeAgent/`generateTestCase`), `phase-2/sequence-diagrams.md §2`, `north-star.md` §2/§4. Downstream: TL re-ticket US-8.x (lệnh `generate` merged đang nhận `--page-source`). |
